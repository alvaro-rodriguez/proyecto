--- 
 - hosts: local
   become: yes
   become_user: root
   
   tasks:

    - name: prueba contenedor centos
      lxc_container:
        name: centos-rebirth
        container_log: true
        template: centos
        state: started

